import { connect } from 'react-redux';

// Actions
//import { selectAlternative } from '../modules/actions';

// Views
import Layout1 from '../presentational/Layout1';


const mapStateToProps = (state, ownProps) => ({
});

const mapDispatchToProps = dispatch => ({
});


export default connect(
  mapStateToProps,
  mapDispatchToProps,
)(Layout1);